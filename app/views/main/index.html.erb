<% if not City.all.empty? %>
  <% City.all.values.each do |value| %>
    <h1> <%= value.name %></h1>
    <p>
      It's <%= value.weather[:temperature] %>°F
      and it looks like <%= value.weather[:description] %>
    </p>
  <% end %>
<% else %>
  <h1>There are no cities added to the app yet. Go to http://localhost:1337/cities/view to add some!</h1>
<% end %>